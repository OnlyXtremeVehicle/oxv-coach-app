# Rapport — LOT V2-L4 · PORTE VOUS (identité, fondateurs, parrainage, réservation OFF)

Date : 19/07/2026 · Prompt : `design-retours/programme-v2/PROMPT_CLAUDE_CODE_V2_L4_VOUS.md`
Prérequis remplis : L0, BE-1, L1 mergés.

---

## Livré — 8 écrans + Sheet fondateur + flux réservation (flag OFF)

### VOUS (`app/(app2)/vous/`)
1. **index** — héros passeport (HeroPhoto véhicule principal, avatar, stats
   RollingCounter), **A2 carte fondateur** (flag `founders`, états
   absent/pending/approved, jauge x/30 réelle `getFoundersCount`), **A3 code**
   (partage natif), 7 sections ListRow Stagger, version app.
2. **profil** — aperçu public + édition inline (même write-path v1
   `addMyPilotMedia` bucket **pilot-media**), opt-in Pavillon.
3. **garage** — FlashList cartes véhicule (HeroPhoto), Sheet véhicule
   (carrousel, specs), ajout véhicule.
4. **carnet** — 4 onglets swipe (Notes+météo réelle / Intentions / Objectifs /
   Programme coach).
5. **equipement** — boîtier (Dial batterie), ceinture, Apple Watch (gate
   HealthKit BE-1, iOS only).
6. **documents** + `document/[doc]` + `decharge` — licence FFSA, waiver (flag
   `pilot_waivers`), lecteurs légaux.
7. **reglages** — 4 groupes : notifications (+rituels B3 réels), **5
   consentements** dont biométrie (révocation immédiate + Sheet confirmation),
   données & sécurité (export, suppression J+30), déconnexion.
8. **support** — tickets + fil + composer.
- **Sheet/écran fondateur** (`vous/fondateur`) — insigne animé, motivation
  20-2000, code parrain préchargeable, `founderService.apply`.

### Réservation A1 (`app/(app2)/reserver/`, flag `app_payments` OFF)
- `index` catalogue (jauge places), `[sessionId]` détail+offres,
  `paiement` (boutons inertes, `// TODO_AVOCAT CGV`). Chaque écran vérifie le
  flag → OFF = « Réservations à l'ouverture — {n}/30 fondateurs ». Analytics
  `reserve_funnel_1/2/3`.
- **`bookingCatalogService`** NOUVEAU **SELECT-only** (grep 0 write) : inspection
  MCP lecture seule préalable confirmée — Heritage 249 000 cents → « 2 490 € »,
  saison inactive → prix « — » (jamais un montant inventé). RLS respectée
  (pilote lit journées publiques + prix actifs seuls).

## Discipline données réelles (agents ont REFUSÉ de fabriquer)
- **Météo carnet** : lecture directe `weather_snapshots` (temperature_c
  nullable) au lieu de `weatherService` qui coerce à 0 — A-WEATHER-1 respecté,
  pas de « 0° du jour » fabriqué.
- **Objectifs** : `pilot_goals` n'a aucun champ de mesure → aucune barre de
  progression affichée (jamais une barre à 0).
- Prix saison inactive → « — ». Insigne licence en `text.hi` (identité commune,
  pas Heritage). `heritage.gold` gated derrière le tier partout.

## Divergences à arbitrer (fondateur) — gaps de SCHÉMA, pas de bugs
1. **Rang fondateur** (A2 approved) : `founder_applications` (BE-1) n'a PAS de
   colonne rang → « FONDATEUR N° {rang:02d} » impossible sans fabriquer. Affiché
   « MEMBRE FONDATEUR » sans ordinal. Pour le N°, ajouter une colonne rang réelle
   (migration BE) + l'exposer dans `getMyApplication`.
2. **Véhicule principal** (garage) : `garageService` n'a NI `is_primary` NI
   `setPrimary`. « En tête » = premier créé (celui qui illustre déjà l'accueil),
   marqué « EN TÊTE » non modifiable. Pour un choix pilote : colonne `is_primary`.
3. **Copier le code A3** : `expo-clipboard` non installé → bouton remplacé par
   partage natif `Share.share` (la feuille expose « Copier »). Copier-1-tap =
   `npx expo install expo-clipboard` + rebuild (petit suivi).

## Preuves
- **tsc 0 · eslint 0 · grep doctrine 0 · grep hex hors tokens 0 · jest 1450
  verts** (logiques pures : founder states, foundersGauge, referral copy,
  gating watch/consent, flag OFF partout, jauge places, formatPrice dont
  Heritage 249000→2490€, révocation consentement capture⇒partage, onglets
  carnet, rituels B3).
- `bookingCatalogService` = SELECT-only vérifié (grep 0 insert/update/rpc).
- **À valider sur device** : swipe onglets carnet, carrousel garage, gate
  HealthKit iOS, luminosité max licence, blur/replis Android.

## Suite (ordre du programme)
L5 CLUB (coaching, territoire, galerie, groupes, convois, carnet Heritage).
