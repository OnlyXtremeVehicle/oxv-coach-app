# Rapport — LOT V2-L2 · PORTE REC (le jour J, 8 écrans)

Date : 19/07/2026 · Prompt : `design-retours/programme-v2/PROMPT_CLAUDE_CODE_V2_L2_REC.md`
Prérequis remplis : L0, BE-1, L1 mergés.

---

## RÈGLE CARDINALE — respectée et prouvée

**`git diff` VIDE sur les 4 fichiers de la chaîne de capture durcie Valence** :
`useAppStateStore.ts`, `captureSessionService.ts`, `captureSyncQueue.ts`,
`bluetoothService.ts`. Les 8 écrans v2 sont une PEAU : ils LISENT l'état
(`useCaptureStep`) ou appellent les MÊMES services que la v1
(`startCaptureSession`, `stopCaptureSession`, `abortCaptureSession`,
`analyzeAndPersistSession`, `addNote`, scan BLE en observation seule). State
machine intacte.

## Les 8 écrans (`app/(app2)/rec/`)

1. **HUB** (`index.tsx`) — `useCaptureStep` (mapping S1..S10 → étape, pur/testé) :
   jour J → redirection à l'étape courante ; hors jour J → HeroPhoto de la
   voiture + Dial countdown (logique réutilisée de `useMiroirHome`, non
   dupliquée) ; aucune inscription → RÉSERVER (fail-closed).
2. **PRÉPARATION** — héros circuit + countdown, météo réelle (absence = ligne
   absente, **A-WEATHER-1**), check-list (état MMKV v1 conservé), QR pass,
   **C1 « Qui roule »** (RPC `session_attendance_public` gatée + opt-in
   `show_attendance`, filtre « Mon groupe »), **C2 convoi** (flag `convoys`
   fail-closed).
3. **ARRIVÉE** — insigne OXV trait animé (une fois/jour, garde MMKV), « JE SUIS
   AU PADDOCK » = navigation (la bascule S5→S7 v1 est pilotée par la géoloc,
   pas un setter).
4. **ÉQUIPEMENT** — scan BLE théâtralisé (services v1 intacts, observation),
   ceinture Polar (coachés, appairage BIO-2), **Sheet consentement biométrie**
   (2 cases → services BE-1 avec garde-fou partage⇒capture ; texte
   `docs/juridique/consentement_biometrie.md` marqué VALIDATION AVOCAT 21/07),
   rappel Watch (flag+consent+iOS, OFF aujourd'hui).
5. **PLACEMENT** — tracé + ligne d'arrivée, sélecteur circuit, **ARMER LA
   CAPTURE** = appui long 600 ms (jauge circulaire, `armementLogic` testé) →
   `startCaptureSession` v1 inchangé.
6. **ROULAGE** — le plus sobre : point REC pulsant + « REC », RIEN d'autre
   (zéro chrono/chiffre/biométrie — Principe 3). Garde d'honnêteté v1 (lien BLE
   décroché signalé sans rouge). « Terminer » → `stopCaptureSession` → fin.
7. **ENTRE-RUNS** — Dial du break (masqué sans départ réel), meilleur tour du
   jour + célébration une-fois (garde `day-record:` distincte de l'all-time),
   note rapide (`addNote` v1), biométrie phase A honnête (fail-closed), tracé
   filigrane 6 %.
8. **FIN** — 4 phases cross-fadées : `fini` (résumé faits réels + PRÉSERVER +
   **BIO-1** idempotent/fail-closed/non-bloquant), `preservation` (MÊME
   `analyzeAndPersistSession` v1 + Dial), `pret` (RecordFlash si record du jour
   + OUVRIR LE BILAN → HeroMorph), `erreur` (StateView + relance). **D4
   incident** : Sheet → `incidentService.report` ; OFFLINE → registre
   `incidentOffline` **SÉPARÉ de captureSyncQueue** (cardinal), rejoué à
   l'ouverture, idempotent.

## Migration C1 (appliquée en prod, committée séparément 6d2b453)
`users.show_attendance` (opt-in, défaut false) + RPC
`session_attendance_public(p_session)` DEFINER gatée : seul un inscrit de la
journée voit les inscrits opt-in (handle/avatar, jamais nom complet).

## Interruptions & reprise
Le build agent a été coupé deux fois (limite de session, puis coupure réseau
mid-response). Les 4 modules de logique de la mission 4 (`entreRunsLogic`,
`finLogic`, `bio1Trigger`, `incidentOffline` + tests) avaient atterri complets ;
seuls les écrans `entre-runs.tsx` et `fin.tsx` manquaient — **écrits à la main**
sur ces logiques déjà testées.

## Preuves
- **`git diff` cardinal = VIDE** (4 fichiers capture). tsc 0 · eslint 0 ·
  grep doctrine 0 · **jest 1294 verts** (129 tests rec : captureStepLogic tous
  les S, armement, garde insigne, day-best, gating biométrie, BIO-1 idempotent
  & non-bloquant, incidentOffline enqueue/rejeu/idempotence, finLogic).
- Consentement biométrie fail-closed (flag OFF → aucun traitement) ; doc
  juridique en attente avocat.
- **À valider sur device** : flux complet S5→pret en mode avion (preuve du
  lot), armement appui long, insigne animé, transitions, blur/replis Android,
  scan BLE réel. Les redirections hub↔étapes pointent bien vers les 8 écrans
  (lot atterri ensemble).

## L2-B — Live Activity (reporté, consigné)
Sous-lot natif iOS (ActivityKit + WidgetKit) : > 1 j de travail natif, **non
inclus** dans ce commit conformément au prompt (« si >1 j natif : reporter, le
noter au rapport »). À traiter comme commit natif séparé.

## Suite (ordre du programme)
L4 VOUS (identité, fondateurs, parrainage, réservation flag OFF).
