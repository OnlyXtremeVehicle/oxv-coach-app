# Rapport — LOT V2-L1 · PORTE MIROIR (3 écrans sensoriels)

Date : 19/07/2026 · Prompt : `design-retours/programme-v2/PROMPT_CLAUDE_CODE_V2_L1_MIROIR.md`
Prérequis remplis : L0 (52bb7bd) + BE-1 (d920d2f) mergés.

---

## Livré

### 1/3 — ACCUEIL MIROIR (`app/(app2)/index.tsx`)
Hook `useMiroirHome` (deux vagues `Promise.allSettled`, services existants
uniquement) + logique pure testée (`decideHomeMode` frontière 7 j exacte,
`seasonFact`, fallbacks photo, `decideReserve` fail-closed, `activeHeritagePack`).
Header condensable (eyebrow PADDOCK/HERITAGE, MIROIR Michroma, avatar → /vous) ;
héros 3 variantes (photo de séance + ChronoHero m celebrate-si-record ; LA
voiture du membre + Dial J-x + météo ; carte RÉSERVER flag OFF → club +
`reserve_intent`) ; signature compacte RadarQdi s ; UN fait (narrative sinon
fait de saison, jamais inventé) ; rangée stats hairline (RECORD millièmes
accent · SAISON km · HERITAGE x/y réel ou SÉANCES) ; bandeau rituel **B3
minimal données réelles** (J-3..J-0 depuis `nextDay`, dismiss swipe persisté,
deep link préparation — la préférence rituels complète arrive en L4) ;
PullToRefreshDial ; squelettes Shimmer ; Stagger 45 ms. Modes capture v1
prioritaires : **S5 ET S6 silencieux** (Principe 3, textes canon v1).

### 2/3 — BILAN (`app/(app2)/bilan/[sessionId].tsx`)
Hook `useBilan` + `bilanLogic` (28 fonctions pures testées). Arrivée HeroMorph
(contrat unifié `bilanHeroMorphId`, provider posé au layout, cible montée dès
le mount) sinon porte ; ChronoHero l + célébration record **une seule fois**
(module partagé `recordCelebration.ts`, garde jamais posée sur données
partielles) ; carte tracé TraceCircuit + puces moments + bande annotation or
(notes de séance d'abord, génériques étiquetées « REPÈRE GÉNÉRAL ») —
**masquée honnêtement si la centerline n'est pas celle du circuit de la
séance** (`fetchSessionCircuitCenterlineExact`, zéro fallback) ; 4 PillarBar
(QDI recalculé au besoin, version ≠ courante → « — ») ; biométrie
**triple-gatée fail-closed** (flag + consentement BE-1 + données) ; debrief
3 actes à provenance pure (généré seul OU fallback intégral, garde doctrine
au rendu, tournures épicènes) ; fil présentiel (bulles par `senderId` réel,
titre honnête « Votre fil avec {coach} ») ; souvenirs + viewer plein écran en
**Modal** (au-dessus de tout, pinch+pan clampés, FERMER accessible,
onAccessibilityEscape) ; cellule vidéo absente tant que `video_overlay` OFF.

### 3/3 — SIGNATURE (`app/(app2)/signature.tsx`)
RadarQdi l théâtral, labels **Cap · Trajectoire · Visée · Plongée ·
Anticipation** — mapping sémantique intérimaire verrouillé par test
(« Trajectoire » = branche trajectoire PARTOUT, un mot = une donnée) ;
`// TODO_ARBITRAGE fondateur` en tête de `signatureLogic.ts` (voir Décisions).
Branches nulles masquées + « x/5 axes mesurés » ; « vous vs vous · 30 jours » ;
Empreinte mensuelle (listMonthlyQdi **filtré à l'algo courant** — les mois
1.0.x aux axes inversés n'existent plus) ; tap mois = morph du grand radar
(withSpring UI thread, pont runOnJS échantillonné ~30 Hz, claquement final
exact, cancel au unmount) ; pilier physio BIO-4 gaté OFF sans teasing ;
« Voir la saison complète » → /data (cible provisoire, TODO_L3_TARGET).

## Vérification adversariale — 34 findings, tous traités

3 lentilles (câblage données réelles / natif-navigation / doctrine-spec).
**17 majeurs corrigés**, dont les violations de la règle données réelles :
- « 0 km / 0 séances » fabriqués sur panne → `loadPilotStats { strict }`,
  absent = « — » ; **état d'erreur réel sur l'accueil** (mode avion ne dit
  plus « Aucune journée au calendrier »).
- Célébration record fabriquée sur panne partielle → record INDÉTERMINÉ =
  pas de célébration, garde non posée.
- HERITAGE x/4 reconstruit → lecture réelle de `heritage_packs`
  (sessions_used/sessions_total du pack actif), dénominateur en dur supprimé.
- QDI 1.0.x invalides affichés (piliers bilan, Empreinte, radar accueil) →
  filtre `QDI_ALGO_VERSION` partout (recalcul paresseux borné : séance <7 j,
  une tentative par lancement).
- Tracé du mauvais circuit présenté comme celui de la séance → masqué.
- Bulles inversées pour un coach qui roule → `senderId === myUserId`.
Et les bugs UI certains : avatar recouvert (overlay z11 tappable aux 2 états),
viewer sous le header (Modal), onScroll JS cassant le worklet
(`useAnimatedScrollHandler`, scrollY écrit sur l'UI thread), collision de
libellés Signature, bandeau B3 absent, silence S6, double-célébration.
17 mineurs corrigés également (deps d'effets, seuils pinch, épicène, source
unique des textes, contraste consigné…).

## Décisions à arbitrer (fondateur)
1. **Labels Signature** (`TODO_ARBITRAGE`, signatureLogic.ts) : mapping
   intérimaire Cap→régularité · Trajectoire→trajectoire · Visée→accélération ·
   Plongée→freinage · Anticipation→fluidité. À confirmer ou re-mapper — le
   verrou de test garantit qu'un changement est un choix explicite.
2. `weatherService` v1 fabrique `temperatureC = 0` sur donnée absente
   (fichier V1 « ne pas réécrire ») — un 0° réel est indiscernable. À traiter
   dans un lot services.

## Preuves
- **tsc 0 · eslint 0 · grep doctrine 0** (seuls hits = tests de la garde
  anti-prescriptif) · **jest 1165 verts** (97 suites), dont **120 tests L1**
  (logiques pures : modes, gating fail-closed, lerpRadar, mapping labels
  verrouillé, B3, heritage pack, provenance debrief).
- Extensions de services ADDITIVES documentées (appelants v1 inchangés) :
  `loadPilotStats{strict}`, `getMyNextTrackDay{strict}`, `listMonthlyQdi`
  (filtre version + strict), `fetchSessionCircuitCenterlineExact`,
  `marginGlobalMeasured`.
- **À valider sur device** (dev-galerie + parcours réel) : fluidité du scroll
  composé accueil, HeroMorph accueil→bilan (géométrie fraîche), morph mensuel
  du radar (profiler release Android), gestes viewer, blur/replis Android.
  Côté accueil, passer les params héros au push (contrat optionnel documenté)
  rendra la cible du morph pleine — petit câblage au polish ou début L2.
- Météo accueil = météo ACTUELLE au circuit (libellé honnête « Météo
  actuelle ») — le service v1 ne fait pas de prévision datée.

## Suite (ordre du programme)
L2 REC (+L2-B Live Activity) — règle cardinale : zéro diff sur
useAppStateStore / captureSessionService / captureSyncQueue / bluetoothService.
